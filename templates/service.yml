apiVersion: v1
kind: Service
metadata:
  name: nginx
  #namespace: roboshop
spec:
  type: {{ .Values.service.type }}
  selector: 
    project: roboshop
    tier: frontend
  ports:
  - protocol: TCP
    port: 80 # service port
    targetPort: 80  # container port